<Page x:Class="MasterFloor.Pages.PartnersPage1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MasterFloor.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PartnersPage1">

    <Grid>
        <DataGrid x:Name="DGPartners" AutoGenerateColumns="False" Background="#F4E8D3">
            <DataGrid.Columns  >
                <DataGridTextColumn Header="Тип" Binding="{ Binding type_partners}" Foreground="#67BA80" />
                <DataGridTextColumn Header="Наименование партнера" Binding="{ Binding name_partners}"/>
                <DataGridTextColumn Header="Скидка" />
                <DataGridTextColumn Header="Директор" Binding="{ Binding director}"/>
                <DataGridTextColumn Header="Номер телефона" Binding="{ Binding phone}"/>
                <DataGridTextColumn Header="Рейтинг" Binding="{ Binding rating}"/>
            </DataGrid.Columns>
            
        </DataGrid>
    </Grid>
</Page>
